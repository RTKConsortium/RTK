# One-step spectral reconstruction

This example shows how to generate simulated spectral projections through an in silico phantom, and then perform a one-step reconstruction.

`````{tab-set}

````{tab-item} Python

```{literalinclude} ./SpectralOneStep.py
:language: python
```
````
`````
